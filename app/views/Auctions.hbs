<div class="col-9">
    <div class="row">
        <h3>Мои аукционы - Участвую</h3>
    </div>
    {{#if Lots}}
    {{#each Lots}}
    <div class="row align-items-center mt-4 py-2" style="border: 2px solid #dee2e6;background-color: #e3effd;">
        <div class="col-2">
            <figure class="figureIndex rounded mx-auto d-block">
                <img src="../{{this.book.photoBooks.0.URL}}" class="rounded mx
            -auto d-block" alt="фото книги ">
            </figure>
        </div>
        <div class="col-4">
            <div class="row align-items-center">
                <div class="col font-weight-bold">
                    {{this.book.title}}
                </div>
            </div>
            <div class="row">Текущая цена: {{this.currentPrice}}</div>
            <div class="row">Кол-во ставок: {{this.numberOfBets}}</div>
            <div class="row">Окончание: {{this.endDate}}</div>
        </div>
        <div class="col-2 mx-3 py-1">
            Ставка: <input type="number" class="form-control" name="myRate{{this.idLot}}" min="{{this.minRate}}"
                value="{{this.minRate}}">
        </div>
        <div class="col- mx-2">
            <div class="row">
                <input type="button" class="btn btn-primary" value="Сделать ставку"
                    onclick="rate({{this.idLot}},{{this.minRate}});">
                </input>
            </div>
            <div class="row my-2">
                <input type="button" class="btn btn-primary" value="Отказаться от участия"
                    onclick="renouncement({{this.idLot}})"></input>
            </div>
        </div>
    </div>
    {{/each}}
    {{else}}
    <div class="row alert alert-primary">
        <div class="col text-center">
            Нам жаль, вы не участвуете ни в одном аукционе :-(
        </div>
    </div>
    {{/if}}
</div>