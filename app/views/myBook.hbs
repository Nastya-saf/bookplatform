<div class="row">
    <div class="col-9">
        <div class="row">
            <h3>Продаваемые мной книги</h3>
        </div>
        {{#if Books}}
        {{#each Books}}
        <div class="row align-items-center mt-4 py-2" style="border: 2px solid #dee2e6;background-color: #e3effd;">
            <div class="col-2">
                <figure class="figureIndex rounded mx-auto d-block">
                    <img src="../{{this.photoBooks.0.URL}}" class="rounded mx-auto d-block" alt="фото книги ">
                </figure>
            </div>
            <div class="col-3">
                {{this.title}}
            </div>
            <div class="col- mr-auto" style="margin: 15px;">
                {{this.price}}
            </div>
            <div class="col- mr-auto">
                {{#if this.lot.idLot}}
                <input class="btn btn-primary" type="button" onclick="auctionOff({{this.lot.idLot}})"
                    value="Снять с аукциона"><br>
                {{else}}
                <input class="btn btn-primary" type="button" onclick="deleteOff({{this.idBook}})"
                    value="Удалить с платформы"><br>
                {{/if}}
            </div>
            <div class="col- mx-2">
                {{#if this.show}}
                {{#if this.paid}}
                {{else}}
                <input class="btn btn-primary" type="button" onclick="pay({{this.idBook}})" value="Оплатить место"><br>
                {{/if}}
                {{/if}}
            </div>
        </div>
        {{/each}}
        {{else}}
        <div class="row alert alert-primary">
            <div class="col text-center">
                У Вас не выставленно ни одной книги на продажу :-(
            </div>
        </div>
        {{/if}}
    </div>
</div>


<script>

</script>